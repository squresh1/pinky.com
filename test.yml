---
- name: install Apache webserver
  hosts: test
  become: yes 
  tasks:
  - name: install the latest version of Apache
    yum:
      name: "*"
      state: latest
  - name: reboot afteryum update
    reboot:
      
